# mkinitcpio.conf for ArchCosta installed system

MODULES=(dm_mod dm_snapshot dm_mirror dm_bufio vmxnet3 vmwgfx vmw_balloon vmw_vmci vsock pvpanic vboxguest vboxsf)
BINARIES=()
FILES=()

HOOKS=(base udev autodetect microcode modconf kms keyboard keymap consolefont block filesystems fsck)

COMPRESSION="zstd"
COMPRESSION_OPTIONS=("-19" "-T0")

# Disable built-in compression detection
KEEP_PXERETRY="yes"
