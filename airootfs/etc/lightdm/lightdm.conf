#
# LightDM Configuration for ArchCosta Live
#

[LightDM]
# Minimum display server timeout
minimum-vt=1
# Don't start the greeter on the same VT as xdmcp sessions
start-default-seat=false
# Always show the greeter
greeter-hide-users=false
# Allow manual login
allow-user-switching=true
allow-guest=false
# Disable guest
guest-session=false
# Check graphical
logind-check-graphical=true
# Autologin
autologin-guest=false
autologin-user=live
autologin-user-timeout=0

[Seat:*]
# Session configuration
session-wrapper=/etc/lightdm/Xsession
greeter-session=lightdm-slick-greeter

# X server configuration
xserver-command=X -core -nolisten tcp
xserver-layout=
xserver-allow-tcp=false

# VT configuration - let lightdm choose
vt-handoff=true

# User themes
user-theme-name=Adwaita
icon-theme-name=Adwaita

# Allow user switching
allow-user-switching=true

# Autologin session - will be detected automatically or set below
autologin-session=hyprland
user-session=hyprland

# Enable wayland support
type=wayland
